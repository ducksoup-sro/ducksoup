namespace Database;

public class Database
{
    // DO NOT DELETE
    // This is needed for the plugins so that they do not have to initialize the Database inside themself, rather more can access the shared one from the main application
}